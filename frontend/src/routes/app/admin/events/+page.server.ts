// import type { Actions, Action } from './$types';

// const createEvent: Action = async ({ request }) => {
//     const formData = request.formData();
// };

// const updateEvent: Action = async ({ request }) => {
//     const formData = request.formData();
// };

// const deleteEvent: Action = async ({ request }) => {
//     const formData = request.formData();
// };

// export const actions: Actions = { createEvent, updateEvent, deleteEvent };
