package offerService

import "context"

func (s *Service) UpdateOffer(ctx context.Context, offer *Offer) error {
	// TODO: get the offer object and check to change the fields that are changed
	return nil
}
